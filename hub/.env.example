# Hub
JWT_SECRET=change-me-jwt-secret
JWT_EXPIRE_HOURS=24
DEVICE_TOKENS=change-me-device-token

# Redis
REDIS_URL=redis://127.0.0.1:6379/0

# App (token que usa la app movil para /auth/login)
APP_TOKEN=change-me-app-token