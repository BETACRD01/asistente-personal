services:
  agentrelay-hub:
    container_name: agentrelay-hub
    build: .
    restart: unless-stopped
    env_file:
      - .env
    network_mode: host
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    mem_limit: 192m
    cpus: "0.50"
    pids_limit: 64